<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.psutipower">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.PsutiPower"
        tools:targetApi="m">
        <activity android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity">

        </activity>

        <activity android:name=".activities.RegistrationActivity">

        </activity>

        <activity android:name=".activities.ChooseOrganizationActivity">

        </activity>

        <activity android:name=".activities.RegistrationOrganizationActivity">

        </activity>

        <activity android:name=".activities.OrganizationMenuActivity">

        </activity>

        <activity android:name=".activities.LoginOrganizationActivity">

        </activity>

        <activity android:name=".activities.ApplicationsFromUsersActivity">

        </activity>

        <activity android:name=".activities.CreateApplicationActivity">

        </activity>

        <activity android:name=".activities.LoginAdminActivity">

        </activity>


        <activity android:name=".activities.UnverifiedOrganizationsActivity">

        </activity>

        <activity android:name=".activities.OneUnverifiedOrganizationActivity">

        </activity>
    </application>
</manifest>