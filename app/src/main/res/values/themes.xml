<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PsutiPower" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:fontFamily">@font/rostov</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:colorBackground">@color/gray_200</item>
        <item name="colorButtonNormal">@color/teal_200</item>
        <item name="buttonTint">@color/green_200</item>
        <item name="colorAccent">@color/green_200</item>
        <!-- Customize your theme here. -->
    </style>
</resources>